namespace RTS {
	public enum CursorState {DefaultCursor, AllyCursor, EnemyCursor, TransparentCursor}
	public enum FlySpawnType {Black, Red, Yellow}
	public enum HealSpawnType {Level_1, Level_2}
	public enum AntSpawnType {Red}
	public enum ItemType {OrganicMatter, Corpse} 
    public enum EAnimation {Null, Idle, Move, Rotate, Spawn, Attack, SpawnerSpecial, Build, Revive, Die, Sit}
}